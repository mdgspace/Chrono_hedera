# Hedera testnet config
HEDERA_ACCOUNT_ID=

# Note: Private key should be stored via `npx hardhat keystore set HEDERA_PRIVATE_KEY`
# (Hardhat 3 built-in keystore plugin) instead of raw text here, per plan.
